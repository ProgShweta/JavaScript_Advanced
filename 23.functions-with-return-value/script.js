function addTwoNum(a, b){
    // console.log(2+3);
    return a + b;

    
}
debugger
const result = addTwoNum(2,3)
console.log(result);

function calculateArea(shape, dimensions){
    console.log(object);
    // Area=length × width ->area of rectangle

    // Area=πr2 -> area of circle
    
    // Area = 1/2*b*h -> area of right angle

}